/**
 * Minimal polyfills file for development.
 * Add necessary imports here (e.g. zone.js) if you rely on them.
 */

// Zone is required by Angular; the dependency is already in package.json
import 'zone.js';
import 'zone.js/dist/zone';